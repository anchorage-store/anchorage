/*
vaulthttpd runs the vault http server.

The vault server owns the overall vault/item structure and keys for users added
to the vault. It also keeps track of what chunks belong to what items.
*/
package main
